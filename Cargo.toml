[package]
name = "my-portfolio"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0-rc.3", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
redis = "0.26"

serde_json = "1.0"

[workspace]
members = ["server", "common"]
